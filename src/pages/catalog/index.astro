---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CategoryCard from '../../components/catalog/CategoryCard.astro';
import { categories } from '../../data/categories';

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Главная", "item": "https://ystroika.ru/" },
        { "@type": "ListItem", "position": 2, "name": "Каталог", "item": "https://ystroika.ru/catalog/" }
      ]
    },
    {
      "@type": "CollectionPage",
      "@id": "https://ystroika.ru/catalog/",
      "name": "Каталог материалов — У-Стройка",
      "description": "Полный каталог кровельных, фасадных материалов и водостоков в Москве и МО.",
      "url": "https://ystroika.ru/catalog/",
      "isPartOf": { "@id": "https://ystroika.ru/#website" },
      "hasPart": categories.map(cat => ({
        "@type": "CollectionPage",
        "name": cat.title,
        "description": cat.description,
        "url": `https://ystroika.ru/catalog/${cat.slug}/`
      }))
    }
  ]
};
---

<BaseLayout
  title="Каталог кровельных и фасадных материалов"
  description="Полный каталог кровельных, фасадных материалов и водостоков в Москве и МО. Металлочерепица, сайдинг, водосток, утепление. Широкий ассортимент, лучшие цены."
>
  <script type="application/ld+json" slot="head" set:html={JSON.stringify(jsonLd)} />

  <div class="bg-slate-50 min-h-screen">
    <!-- Хлебные крошки + заголовок -->
    <div class="bg-white border-b border-slate-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <nav class="flex items-center gap-2 text-sm text-slate-500 mb-3">
          <a href="/" class="hover:text-orange-500 transition-colors">Главная</a>
          <svg class="w-3.5 h-3.5 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
          <span class="text-slate-900 font-medium">Каталог</span>
        </nav>
        <h1 class="text-2xl lg:text-3xl font-bold text-slate-900">Каталог материалов</h1>
        <p class="mt-1.5 text-slate-500">
          {categories.length} разделов · Кровля, фасад, водосток и многое другое · Москва и МО
        </p>
      </div>
    </div>

    <!-- Сетка разделов -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {categories.map(cat => (
          <CategoryCard
            slug={cat.slug}
            title={cat.title}
            description={cat.description}
            icon={cat.icon}
            subcategoriesCount={cat.subcategories?.length ?? 0}
          />
        ))}
      </div>

      <!-- CTA -->
      <div class="mt-12 bg-slate-900 text-white rounded-2xl p-8 flex flex-col sm:flex-row items-center justify-between gap-6">
        <div>
          <p class="font-bold text-xl">Не нашли нужный материал?</p>
          <p class="text-slate-400 text-sm mt-1 max-w-sm">
            Позвоните — менеджер подберёт по вашим характеристикам и бюджету
          </p>
        </div>
        <a
          href="tel:88003339495"
          class="shrink-0 inline-flex items-center gap-2 bg-orange-500 hover:bg-orange-600 text-white font-semibold px-6 py-3 rounded-xl transition-colors"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
          </svg>
          8-800-333-94-95
        </a>
      </div>
    </div>
  </div>
</BaseLayout>
